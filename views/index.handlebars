<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Eat Da Burger!</title>
    <link href="https://fonts.googleapis.com/css?family=Give+You+Glory&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="assets/css/style.css" type="text/css">
</head>

<body>
    <img class="burger" src="assets/images/burger.png">
    <h1>Eat Da Burger Menu!</h1>

    <div id="burgers-available">
        <div class="uneaten">
            <h3 class="available"> Ready To Be Eaten Burgers </h3>
            
            {{#each burgers}}
            {{#unless devoured}}
            {{>eatBurger eaten=false}}
            {{/unless}}
            {{/each}}


        </div>

        <div class="form-group">
            <h3 class="yummy"> These Burgers Have Been Eaten </h3>
            <label for="devoured">Devoured</label><br>
            {{#each burgers}}
            {{#if devoured}}
            {{>eatBurger eaten=true}}
            {{/if}}
            {{/each}}

        </div>
    </div>

    <h2>Add a Burger</h2>
    <form id="create-form">

    <div class="form-group">
        <label for="burg">Burger Name:</label>
        <input type="text" id="burg" name="name">
    </div>

    <button type="submit">Add Burger</button>
    </form>

    